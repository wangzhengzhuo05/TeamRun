# Require Accountable Team Tasks for Development Runs

Every Developer Agent Run must belong to a Team Task with frozen context and an accountable Owner or Admin; a Team Agent never owns the task itself. This trades unscoped prompt convenience for a durable chain from initiator and task through Agent Run, branch, pull request, and audit history, while Members remain limited to questions and Documentation Actions.
